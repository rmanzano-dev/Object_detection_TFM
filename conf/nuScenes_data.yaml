# dataset.yaml
train: ../NuScenes_YOLO/train  # Ruta a tus imágenes de entrenamiento
val: ../NuScenes_YOLO/validation      # Ruta a tus imágenes de validación

nc: 3  # Número de clases, por ejemplo, vehículos
names: ['vehicle', 'pedestrian', 'cyclist']  # Las clases